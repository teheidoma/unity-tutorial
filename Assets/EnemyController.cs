using UnityEngine;

public class EnemyController : MonoBehaviour {
    public float hp = 100;
    
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        
    }
}
